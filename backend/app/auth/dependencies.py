# dependencies.py — REMOVED (Firebase handles auth + role-based access)
#
# Authentication is now handled by Firebase Auth SDK in each mobile app.
# Role-based access control is enforced by Firestore Security Rules.
#
# See FIREBASE_SETUP.md → Step 5 for security rules that check user roles.
