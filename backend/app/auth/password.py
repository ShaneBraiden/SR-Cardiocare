# password.py — REMOVED (Firebase Auth handles password hashing)
#
# Firebase Auth manages password hashing and validation server-side.
# Password requirements can be configured in Firebase Console:
#   Authentication → Settings → Password policy
#
# Client-side validation is done in the mobile apps:
#   - iOS: AuthManager.swift → isValidPassword()
#   - Android: AuthManager.kt → isValidPassword()
