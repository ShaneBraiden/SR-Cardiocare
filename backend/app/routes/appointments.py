# appointments.py - REMOVED (Firestore /appointments collection replaces this)
#
# Appointments stored in /appointments/{id}
# Query by doctor: .whereField("doctorId", isEqualTo: uid)
# Query by patient: .whereField("patientId", isEqualTo: uid)
