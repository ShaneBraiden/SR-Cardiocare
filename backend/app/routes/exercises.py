# exercises.py - REMOVED (Firestore /exercises collection replaces this)
#
# Exercises stored in /exercises/{id}
# Filter by category: .whereField("category", isEqualTo: category)
# Created by doctors, readable by all authenticated users
