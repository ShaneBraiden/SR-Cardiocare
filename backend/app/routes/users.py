# users.py - REMOVED (Firestore /users collection replaces this)
#
# User profile is stored in Firestore at /users/{uid}
# Mobile apps read/write directly using Firebase SDK:
#   iOS:  Firestore.firestore().collection("users").document(uid)
#   Android: Firebase.firestore.collection("users").document(uid)
