# workouts.py - REMOVED (Firestore /workouts collection replaces this)
#
# Workout sessions stored in /workouts/{id}
# Feedback stored in subcollection: /workouts/{id}/feedback/{fbId}
# Query by patient: .whereField("patientId", isEqualTo: uid)
